# Copy to example/.env.local (gitignored) and point it at your own backend.
# EXPO_PUBLIC_* variables are inlined by Expo/Metro at build time.
EXPO_PUBLIC_API_URL=https://your-server.example.com
EXPO_PUBLIC_DEVICE_ID=demo-device
